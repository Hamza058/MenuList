@using EntityLayer.Concrete
@model List<Product>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script>
        function loadProducts() {
            $("#loading").show();
            $.ajax({
                type: "Get",
                url: "/Category/Gett/",
                data: { name: "" },
                success: function (data) {
                    $("#categories").html("");
                    $("#products").html("");
                    $.each(data, function (index, item) {
                        let product = `
                                    <input class="dropdown" type="checkbox" id="${index}" name="${index}" />
                                    <label class="for-dropdown" for="${index}">${item.categoryName}</label>
                                    <div id="qqq" class="section-dropdown mb-3"> </div>`

                        $("#categories").append(product);
                        $.each(item.products, function (index, item) {
                            let saas =
                                `<div class="row-cols-fluid-4 text-center mb-1">
                                        <img src="/img/${item.image}" class="border border-dark m-2" width="60%" />
                                        <div class="col-lg-12 border-bottom border-dark pb-2"><h4>${item.productName}</h4></div>
                                        <div class="col-lg-12 text-start text-start pt-1 ps-1 pe-1">${item.description}</div>
                                        <div class="col-lg-12 text-end pe-2 border-bottom border-light">${item.price}</div>
                                </div>
                                                `;
                            $("#qqq").append(saas);
                        });
                    });
                    $("#loading").hide();
                },
                error: function (e) {
                    console.log(e);
                }
            })
        }
        loadProducts();
    </script>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="~/css/style.css">
</head>
<body>
    <div class="light-back"></div>
    <div id="categories" class="sec-center">
        <div id="loading" class="text-center">
            <div class="spinner-border text-light" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
</body>
</html>